import React from 'react';
import LoginForm from '@/components/form/login-form';

export default function LoginPage() {
  return (
    <main className="mx-auto p-4 container relative h-full flex items-center justify-center flex-grow-1">
      <LoginForm />
    </main>
  );
}
