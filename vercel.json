{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install && npm run preinstall",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/services/ui/$1"
    }
  ]
}
